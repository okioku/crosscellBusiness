# Local-only defaults. Copy to .env and never commit the resulting .env file.
AWS_PROFILE=ring-dev
AWS_REGION=ap-northeast-1
BEDROCK_MODEL_ID=jp.amazon.nova-2-lite-v1:0

DATABASE_NAME=ring
AURORA_CLUSTER_ARN=
AURORA_SECRET_ARN=

HOTPEPPER_API_KEY=
JALAN_PROVIDER=mock
BOOKING_PROVIDER=mock

VITE_API_BASE_URL=
VITE_COGNITO_USER_POOL_ID=
VITE_COGNITO_CLIENT_ID=
VITE_COGNITO_DOMAIN=
